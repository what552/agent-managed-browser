# 项目任务说明（TASK）

> 用途：定义项目目标、范围与交付标准，作为多 Agent 协作的统一“任务合同”。

## 1) 目标（Goal）
- **项目名称**：`openclaw-browser`
- **项目定位**：面向多 Agent 的本地浏览器运行时（Browser Runtime），让 Agent 不依赖 relay 即可直接调用 Chromium 完成搜索、浏览、提取与登录态复用。
- **核心业务目标**：
  - 为 OpenClaw/LangGraph/通用脚本提供统一 CLI 能力，降低 Agent 集成浏览器的复杂度。
  - 支持“人工登录 + 自动执行”的混合模式：登录时可见浏览器接管，平时无头自动运行。
  - 提供可追溯输出：关键操作有证据片段与审计记录，满足投研与业务合规场景。
  - 以 Linux 首版为优先，确保可安装、可运行、可调试、可扩展。
- **目标用户**：
  - 需要让 Agent 访问真实网页并可登录站点的开发者/平台团队。
  - 需要“登录后自动化浏览与提取”能力的多用户服务系统（如 LangGraph 编排服务）。
- **关键使用场景**：
  - Agent 自动搜索并打开目标页面，抽取结构化内容返回上层工作流。
  - 站点登录态过期时，系统提示用户完成可视化登录后自动恢复任务。
  - 同一 profile 下连续执行多步任务，保证上下文连续与审计可追踪。
- **成功指标（KPI）**：
  - 指标1（可用性）：`MVP 核心命令（login/search/open/extract）端到端成功率 >= 95%`
  - 指标2（稳定性）：`同一 profile 的登录态复用成功率 >= 90%（24小时内）`
  - 指标3（集成效率）：`新 Agent 接入 CLI 后 2 小时内完成最小链路联调`
  - 指标4（可追溯性）：`100% 命令调用生成 audit_id，关键结果包含 evidence`
  - 指标5（交付节奏）：`MVP 在 3 周内达到可演示状态（Linux）`

## 2) MVP 范围（In Scope）
- **必须交付（P0）**：
  - `<核心能力A>`
  - `<核心能力B>`
  - `<可验证输出C>`
- **可选交付（P1）**：
  - `<增强项A>`
  - `<增强项B>`

## 3) 非目标（Out of Scope）
- `<不在本迭代处理的模块/场景>`
- `<与当前目标无直接关系的优化>`
- `<高风险、低收益实验项>`

## 4) 验收标准（Acceptance Criteria）
- **功能验收**：
  - [ ] 所有 P0 项均有可演示路径
  - [ ] 关键流程具备明确输入/输出
- **质量验收**：
  - [ ] 无阻塞级缺陷（Blocker）
  - [ ] 关键模块有基础测试或可验证脚本
- **文档验收**：
  - [ ] `CONTEXT.md`、`ARCHITECTURE.md`、`DECISIONS.md`、`TODO.md` 已同步
  - [ ] 评审报告模板可用于复盘

## 5) 约束（Constraints）
- **时间约束**：`<截止日期 / 迭代周期>`
- **资源约束**：`<人力、预算、算力、API 配额等>`
- **技术约束**：`<语言/框架/基础设施限定>`
- **合规约束**：`<隐私、安全、审计要求>`

## 6) 风险（Risks）
| 风险 | 影响 | 概率 | 应对策略 | 负责人 |
|---|---|---|---|---|
| `<风险描述1>` | 高/中/低 | 高/中/低 | `<预防与应急>` | `<Owner>` |
| `<风险描述2>` | 高/中/低 | 高/中/低 | `<预防与应急>` | `<Owner>` |

## 7) 里程碑（Milestones）
| 里程碑 | 时间 | 产出 | 验收人 |
|---|---|---|---|
| M1：需求冻结 | `<YYYY-MM-DD>` | `TASK/CONTEXT 定稿` | `<Reviewer>` |
| M2：方案评审 | `<YYYY-MM-DD>` | `ARCHITECTURE/DECISIONS 初版` | `<Reviewer>` |
| M3：MVP 联调 | `<YYYY-MM-DD>` | `可演示版本` | `<Reviewer>` |
| M4：发布准备 | `<YYYY-MM-DD>` | `发布检查清单` | `<Reviewer>` |

## 8) 变更记录（Change Log）
| 日期 | 变更内容 | 变更人 |
|---|---|---|
| `<YYYY-MM-DD>` | `初始化模板` | `<Name>` |
| `2026-02-25` | `细化项目目标、用户场景与KPI` | `Codex` |
